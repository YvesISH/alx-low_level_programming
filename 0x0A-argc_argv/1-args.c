#include <stdio.h>
#include "main.h"
/**
 * main - print the name f the program
 * @argc: count arguments
 * @argv: Arguments
 *
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
	void argv;

	printf("%i\n", argc - l);

	return (0);
}

#include "main.h"
#include <stdio.h>
/** 
 * _abs -it computes the absolute value
 * @n: integer input
 * Return: absolute value
 */
int _abs(int n)
{
	if (n > 0 || c == 0)
	{
		return(c);
	}
	else
		return (c * -1);
}
